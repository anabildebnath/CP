#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long n;
    cin >> n;
    long long x = 2;

    vector<long long> v;

    while (n > 1)
    {
        if (n % x ==0)// checking if the first prime number(2) is factor or not 
        {
            v.push_back(x);
            n /= x;//then dividing the number to create a smaller number to check 
        }
        else
        {
            x++;//if the number can not be divided by the prime number then we do to the next prime number 
        }
    }
    for (int i = 0; i < v.size(); i++)
    {
        cout << v[i] << " ";
    }
    cout << endl;
}
